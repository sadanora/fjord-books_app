<h1><%= User.model_name.human %></h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<%= paginate @users %>

<br>

<%= link_to t('.current_user_info'), current_user %>
<%= link_to t('.edit_current_user_info'), edit_user_registration_path %>
<%= link_to t('views.common.logout'), destroy_user_session_path, method: :delete %>

<br>

<%= link_to t(Book.model_name.human), books_path %>
